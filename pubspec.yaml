name: frideos_general
description: Example app to show some of the features of the frideos library.
version: 1.0.0+2

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter  
  frideos: ^0.6.1
    
dev_dependencies:
  flutter_test:
    sdk: flutter  


flutter:
  uses-material-design: true
